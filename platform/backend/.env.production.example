PORT=4000
NODE_ENV=production

DATABASE_URL=postgresql://<user>:<password>@<host>:5432/<database>
# GitHub Pages (проєкт) + локальні порти; додай свій прод-домен через кому
CORS_ALLOWED_ORIGINS=https://vlad-pm797.github.io,http://localhost:5173,http://127.0.0.1:5500

JWT_ACCESS_SECRET=replace_with_strong_random_secret
JWT_REFRESH_SECRET=replace_with_strong_random_secret
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL_DAYS=30
REMOTE_TEST_INVITE_KEY=
